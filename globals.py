# Runtime-logic related
PATH_FLAG = "/Users/Mehmet/Desktop/frost"
KEYWORD_FLAG = "@Service"
EXTENSION_FLAG = ".java"

# CL Interface related
ENTRY_TEXT = """A simple script to search for how many Service classes you have in your project.
The default parameters are set inside the globals.py file
The parser will still look for your arguments, however if no argument has been specified with a given flag, 
the program will set it to the global value."""